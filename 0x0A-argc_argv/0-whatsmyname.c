#include <stdlib.h>
#include <stdio.h>

/**
 * main - program prints name followed by a new line
 * @argc: argument count
 * @argv: string array vector
 * Return: nothing
 */

int main(int argc, char *argv[])
{
	(void)argc;
	printf("%s\n", argv[0]);
	return (0);
}
