#include "main.h"
/**
 * _abs - computes the absolute value of an integer
 * @b: integer to be checked
 * Return: returns absolute value
 */
int _abs(int b)
{
	if (b < 0)
	{
	(b = b * -1);

		return (b);
	}
	else
		return (b);
	return (0);
}
